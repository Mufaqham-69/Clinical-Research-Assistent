services:
     - type: web
       name: cura-link-frontend
       buildCommand: npm install && npm run build
       startCommand: npm install -g serve && serve -s build -p $PORT
       envVars:
         - key: REACT_APP_API_URL
           value: https://clinical-research-assistent-1.onrender.com/